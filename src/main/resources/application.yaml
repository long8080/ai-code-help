spring:
  application:
    name: ai-code-help
  profiles:
    active: local
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

server:
  port: 8081
  servlet:
    context-path: /api
